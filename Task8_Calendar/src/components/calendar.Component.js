var calendarComponent = new Component()
    .setTag("div")
    .setAttributeName("id")
    .setAttributeValue("js-calendar")
    .setClassName("calendar page__calendar")
    .setContent(null)

var calendarNode = render.createNode(calendarComponent);