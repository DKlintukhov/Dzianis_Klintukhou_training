var daysOfWeekComponent = new Component()
    .setTag("div")
    .setAttributeName("id")
    .setAttributeValue("js-daysOfWeeks")
    .setClassName("calendar__days")
    .setContent(null)

var daysOfWeekNode = render.createNode(daysOfWeekComponent);